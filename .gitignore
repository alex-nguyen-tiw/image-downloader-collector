.claude/

# Build output — regenerated by build.ps1, never edit or commit
dist/

# Shipped release artifacts ARE kept for trace-back / store audit.
# Ignore stray zips everywhere else.
*.zip
!releases/**/*.zip
